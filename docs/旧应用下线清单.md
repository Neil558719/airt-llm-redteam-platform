# 旧应用下线清单

更新时间：2026-08-23

统一 Chatflow 已成为主目标，以下旧应用仅保留为回归对照，状态为 `deprecated`：

| 旧目标 | 当前处理 | 删除状态 | 说明 |
|---|---|---|---|
| Airt Dify 纯文本 | 保留只读回归对照 | 待确认 | 旧应用曾回答与知识库不一致的“7 天”，不能作为正确基线 |
| LLM 框架 Dify 客服 | 保留兼容入口 | 待确认 | 已有旧 pytest 用例仍可运行 |
| LLM 框架 FastGPT 客服 | 保留扩展适配器 | 待确认 | `examples/kb_bots/adapter_fastgpt.py` 继续维护 |

删除上述应用及其知识库前，需要确认：

1. 不再需要旧报告和回归对照；
2. 已导出 API 配置、测试结果和知识库备份；
3. 接受删除后无法直接恢复线上应用；
4. 明确回复“确认删除旧应用”。

在收到明确确认前，本项目不会执行删除。
